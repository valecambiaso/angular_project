import { PromocionadoDirectiveDirective } from './promocionado-directive.directive';

describe('PromocionadoDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new PromocionadoDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});

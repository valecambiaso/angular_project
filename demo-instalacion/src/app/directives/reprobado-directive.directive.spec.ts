import { ReprobadoDirectiveDirective } from './reprobado-directive.directive';

describe('ReprobadoDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ReprobadoDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});

import { AprobadoDirectiveDirective } from './aprobado-directive.directive';

describe('AprobadoDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new AprobadoDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
